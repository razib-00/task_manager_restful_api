/*
import 'package:flutter/material.dart';

import '../../../Style/color_style.dart';
import '../../../Style/text_message_style.dart';

class TaskBodyFunction extends StatelessWidget {
  const TaskBodyFunction({super.key, this.addNewTaskModel});
  final AddNewTaskModel? addNewTaskModel;

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(
          addNewTaskModel?.data?.description??'',
          style: head7Text(colorLightBlack),
        ),
        const SizedBox(height: 4),
        Text(
          addNewTaskModel?.data?.createdDate??'',
          style: head7Text(colorBlack),
        ),
      ],
    );
  }
}
*/
